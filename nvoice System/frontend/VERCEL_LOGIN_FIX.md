# 🔧 حل مشكلة تسجيل الدخول على Vercel

## 📋 المشكلة
التطبيق المنشور على Vercel لا يستطيع الاتصال بالـ Backend، مما يمنع تسجيل الدخول.

## 🎯 السبب
1. **متغير البيئة `VITE_API_URL` غير مضبوط** في Vercel
2. الكود يستخدم قيمة افتراضية خاطئة عند عدم وجود المتغير
3. إعدادات CORS في Backend قد لا تسمح بالوصول من نطاق Vercel

---

## ✅ الحل الكامل

### الخطوة 1: إضافة متغير البيئة في Vercel

1. **افتح مشروعك في Vercel**:
   - اذهب إلى: https://vercel.com/dashboard
   - اختر المشروع الخاص بك

2. **اذهب إلى الإعدادات**:
   - اضغط على **Settings** (الإعدادات)
   - اختر **Environment Variables** (متغيرات البيئة)

3. **أضف المتغير**:
   ```
   Name:  VITE_API_URL
   Value: https://your-backend.onrender.com
   ```
   
   ⚠️ **مهم جداً**:
   - استبدل `https://your-backend.onrender.com` برابط الـ Backend الحقيقي الخاص بك على Render
   - **لا تضع** slash `/` في نهاية الرابط
   - تأكد أن الرابط يبدأ بـ `https://`

4. **احفظ التغييرات**:
   - اضغط **Save**

---

### الخطوة 2: تحديث إعدادات CORS في Backend

1. **افتح ملف `settings.py` في Backend**:
   ```
   d:\ZahranTeck\Invoice Governance System\nvoice System\backend\core\settings.py
   ```

2. **ابحث عن `CORS_ALLOW_ALL_ORIGINS`** (السطر 147):
   
   **الكود الحالي**:
   ```python
   CORS_ALLOW_ALL_ORIGINS = True
   ```

3. **استبدله بالكود التالي** (أكثر أماناً):
   ```python
   # CORS Settings - Production
   CORS_ALLOW_ALL_ORIGINS = False
   CORS_ALLOWED_ORIGINS = [
       'https://zahran-system-ci9dzwtx4-zahrans-projects-7fe13656.vercel.app',  # رابط Vercel الخاص بك
       'http://localhost:5173',  # للتطوير المحلي
       'http://localhost:8000',  # للتطوير المحلي
   ]
   ```

   ⚠️ **استبدل** رابط Vercel بالرابط الفعلي لتطبيقك

4. **احفظ الملف**

---

### الخطوة 3: إعادة نشر Backend على Render

1. **ادفع التغييرات إلى Git**:
   ```powershell
   git add .
   git commit -m "Update CORS settings for Vercel"
   git push origin master
   ```

2. **Render سيقوم بإعادة النشر تلقائياً**
   - انتظر 2-3 دقائق حتى يكتمل النشر

---

### الخطوة 4: إعادة نشر Frontend على Vercel

بعد إضافة متغير البيئة، يجب إعادة نشر التطبيق:

**الطريقة 1: إعادة النشر التلقائي**
1. اذهب إلى **Deployments** في Vercel
2. اضغط على **Redeploy** للنشر الأخير
3. اختر **Redeploy**

**الطريقة 2: دفع تحديث جديد**
```powershell
git commit --allow-empty -m "Trigger Vercel redeploy"
git push origin master
```

---

## 🧪 اختبار الحل

بعد إعادة النشر:

1. **افتح التطبيق** على Vercel:
   ```
   https://zahran-system-ci9dzwtx4-zahrans-projects-7fe13656.vercel.app/
   ```

2. **افتح Developer Tools** في المتصفح:
   - اضغط `F12`
   - اذهب إلى **Console**

3. **جرب تسجيل الدخول**:
   - اسم المستخدم: `admin`
   - كلمة المرور: `admin`

4. **تحقق من الأخطاء**:
   - إذا ظهرت أخطاء CORS → راجع الخطوة 2
   - إذا ظهرت أخطاء Network → تحقق من رابط Backend
   - إذا ظهرت أخطاء 401/403 → المشكلة في المصادقة

---

## 🔍 كيفية معرفة رابط Backend الخاص بك

إذا لم تكن متأكداً من رابط Backend:

1. **اذهب إلى Render Dashboard**:
   - https://dashboard.render.com/

2. **اختر خدمة Backend**

3. **انسخ الرابط** من الأعلى:
   - سيكون بالشكل: `https://your-service-name.onrender.com`

---

## 📊 التحقق من نجاح الحل

### علامات النجاح:
- ✅ صفحة تسجيل الدخول تظهر بشكل صحيح
- ✅ عند إدخال بيانات خاطئة، تظهر رسالة خطأ من Backend
- ✅ عند إدخال بيانات صحيحة، يتم تسجيل الدخول بنجاح
- ✅ لا توجد أخطاء CORS في Console

### علامات الفشل:
- ❌ رسالة "Failed to fetch" في Console
- ❌ رسالة "CORS policy" في Console
- ❌ لا يحدث شيء عند الضغط على زر الدخول
- ❌ الصفحة تبقى في حالة "جاري التحقق..." إلى الأبد

---

## 🆘 إذا استمرت المشكلة

### الفحص 1: تحقق من متغير البيئة
في Vercel Console، افتح:
```
Settings → Environment Variables
```
تأكد أن `VITE_API_URL` موجود وصحيح

### الفحص 2: تحقق من Backend
افتح في المتصفح:
```
https://your-backend.onrender.com/api/auth/login/
```
يجب أن تظهر رسالة JSON (حتى لو كانت خطأ)

### الفحص 3: تحقق من CORS
في Developer Tools → Network → اختر طلب فاشل → Headers
ابحث عن:
```
Access-Control-Allow-Origin
```

---

## 📝 ملاحظات مهمة

1. **متغيرات البيئة تحتاج إعادة نشر**:
   - أي تغيير في Environment Variables يتطلب Redeploy

2. **CORS يجب أن يتطابق تماماً**:
   - الرابط في CORS يجب أن يكون مطابقاً 100% لرابط Vercel
   - لا تنسى `https://`
   - لا تضع `/` في النهاية

3. **Render قد يستغرق وقتاً**:
   - بعد دفع التغييرات، انتظر 2-3 دقائق
   - تحقق من Logs في Render للتأكد من نجاح النشر

---

## 🎯 الخلاصة

المشكلة الرئيسية هي **عدم وجود متغير البيئة** في Vercel. بعد إضافته وإعادة النشر، سيعمل تسجيل الدخول بشكل صحيح.

**الخطوات الأساسية**:
1. ✅ أضف `VITE_API_URL` في Vercel
2. ✅ حدّث CORS في Backend
3. ✅ أعد نشر Backend على Render
4. ✅ أعد نشر Frontend على Vercel
5. ✅ اختبر تسجيل الدخول

---

**تاريخ الإنشاء**: 13 فبراير 2026  
**الحالة**: جاهز للتطبيق ✅
