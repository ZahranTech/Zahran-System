# 🔐 ZahranTeck OTP - نظام مصادقة ثنائية احترافي

<div align="center">

![Status](https://img.shields.io/badge/Status-Production%20Ready-success)
![Django](https://img.shields.io/badge/Django-6.0.2-green)
![React](https://img.shields.io/badge/React-18+-blue)
![Security](https://img.shields.io/badge/Security-2FA%20Enabled-red)

**نظام مصادقة OTP متطور على مستوى المؤسسات مع واجهة مستخدم وتجربة مستخدم مذهلة**

[الميزات](#features) • [التثبيت](#installation) • [الاستخدام](#usage) • [الهندسة المعمارية](#architecture) • [لقطات الشاشة](#screenshots)

</div>

---

## ✨ الميزات (Features)

### 🎯 الوظائف الأساسية
- ✅ **2FA المستند إلى TOTP** - كلمة مرور لمرة واحدة تستند إلى الوقت بمعايير الصناعة (RFC 6238)
- ✅ **توليد رمز QR** - إعداد فوري مع أي تطبيق مصادقة
- ✅ **دعم أجهزة متعددة** - إدارة أجهزة مصادقة متعددة
- ✅ **إدارة الأجهزة** - إضافة وعرض وإلغاء الأجهزة فورًا
- ✅ **مصادقة JWT** - مصادقة آمنة قائمة على الرموز
- ✅ **إدارة الجلسة** - تحديث تلقائي للرموز ومعالجة انتهاء الصلاحية

### 🎨 تصميم متميز
- ✅ **واجهة Glassmorphism** - جمالية عصرية تشبه الزجاج المفروم
- ✅ **تدرجات متحركة** - خلفيات ديناميكية وجذابة
- ✅ **Framer Motion** - رسوم متحركة سلسة واحترافية
- ✅ **تصميم متجاوب** - مثالي على سطح المكتب والجهاز اللوحي والجوال
- ✅ **الوضع الداكن** - مريح للعينين ومظهر احترافي
- ✅ **تفاعلات دقيقة** - تأثيرات تحويم وانتقالات ممتعة

### 🔒 ميزات الأمان
- ✅ **تشفير المفتاح السري** - لكل جهاز سر فريد
- ✅ **تدوير OTP كل 30 ثانية** - تنتهي صلاحية الرموز تلقائيًا
- ✅ **الإلغاء الفوري** - تعطيل الأجهزة المخترقة فورًا
- ✅ **حماية CORS** - طلبات آمنة عبر الأصول
- ✅ **القائمة السوداء للرموز** - تسجيل الخروج يبطل الرموز
- ✅ **التحقق من صحة كلمة المرور** - مدققات Django المدمجة

---

## 🚀 التثبيت (Installation)

### المتطلبات الأساسية
- Python 3.10+
- Node.js 18+
- npm أو yarn

### إعداد الواجهة الخلفية (Backend Setup)

1. **انتقل إلى دليل الواجهة الخلفية:**
   ```bash
   cd backend
   ```

2. **قم بتنشيط البيئة الافتراضية:**
   ```bash
   .\venv\Scripts\activate  # Windows
   source venv/bin/activate  # Linux/Mac
   ```

3. **تثبيت الاعتماديات** (مثبتة بالفعل):
   ```bash
   pip install django djangorestframework django-cors-headers djangorestframework-simplejwt pyotp qrcode pillow
   ```

4. **تشغيل الترحيلات** (تم بالفعل):
   ```bash
   python manage.py migrate
   ```

5. **إنشاء مستخدم تجريبي** (تم إنشاؤه بالفعل):
   ```bash
   python manage.py create_demo_user
   ```

6. **بدء الخادم:**
   ```bash
   python manage.py runserver
   ```
   ستعمل الواجهة الخلفية على: **http://localhost:8000**

### إعداد الواجهة الأمامية (Frontend Setup)

1. **انتقل إلى دليل الواجهة الأمامية:**
   ```bash
   cd frontend
   ```

2. **تثبيت الاعتماديات** (مثبتة بالفعل):
   ```bash
   npm install
   ```

3. **بدء خادم التطوير:**
   ```bash
   npm run dev
   ```
   ستعمل الواجهة الأمامية على: **http://localhost:5173**

---

## 📖 الاستخدام (Usage)

### بيانات الدخول التجريبية
```
Username: demo
Password: Demo@123
```

### تدفق المستخدم

1. **تسجيل الدخول** ← أدخل اسم المستخدم وكلمة المرور
2. **إعداد 2FA** ← امسح رمز QR باستخدام Google/Microsoft Authenticator
3. **التحقق** ← أدخل الرمز المكون من 6 أرقام من تطبيقك
4. **لوحة التحكم** ← إدارة أجهزتك وعرض حالة الأمان

### تطبيقات المصادقة المدعومة
- ✅ Google Authenticator
- ✅ Microsoft Authenticator
- ✅ Authy
- ✅ أي تطبيق متوافق مع TOTP

---

## 🏗️ الهندسة المعمارية (Architecture)

### مكدس التكنولوجيا

#### الواجهة الخلفية (Backend)
- **Framework:** Django 6.0.2
- **API:** Django REST Framework
- **Authentication:** Simple JWT
- **OTP:** PyOTP (تنفيذ TOTP)
- **QR Codes:** qrcode + Pillow
- **CORS:** django-cors-headers

#### الواجهة الأمامية (Frontend)
- **Framework:** React 18
- **Build Tool:** Vite
- **Routing:** React Router DOM
- **Styling:** Tailwind CSS
- **Animations:** Framer Motion
- **Icons:** Lucide React
- **HTTP Client:** Axios

### هيكل المشروع

```
OTP/
├── backend/
│   ├── authentication/          # Main app
│   │   ├── models.py           # TOTPDevice model
│   │   ├── views.py            # API endpoints
│   │   ├── serializers.py      # Data validation
│   │   └── urls.py             # URL routing
│   ├── otp_core/               # Django project
│   │   ├── settings.py         # Configuration
│   │   └── urls.py             # Main routing
│   └── manage.py
│
├── frontend/
│   ├── src/
│   │   ├── pages/              # Page components
│   │   │   ├── Login.jsx
│   │   │   ├── Setup2FA.jsx
│   │   │   ├── VerifyOTP.jsx
│   │   │   └── Dashboard.jsx
│   │   ├── services/
│   │   │   └── api.js          # API service
│   │   ├── App.jsx             # Main app
│   │   ├── main.jsx            # Entry point
│   │   └── index.css           # Global styles
│   └── package.json
│
└── README_AR.md
```

---

## 🔐 نقاط نهاية API

### المصادقة

| الطريقة | نقطة النهاية | الوصف | المصادقة مطلوبة |
|--------|----------|-------------|---------------|
| POST | `/api/auth/login/` | تسجيل الدخول ببيانات الاعتماد | لا |
| GET | `/api/auth/setup-2fa/` | الحصول على رمز QR للإعداد | نعم |
| POST | `/api/auth/setup-2fa/` | التحقق وتفعيل 2FA | نعم |
| POST | `/api/auth/verify-2fa/` | التحقق من OTP أثناء تسجيل الدخول | نعم |
| GET | `/api/auth/devices/` | سرد جميع الأجهزة | نعم |
| DELETE | `/api/auth/devices/{id}/` | حذف جهاز | نعم |

---

## 🎨 أبرز ميزات التصميم

### لوحة الألوان
- **الأساسي:** أزرق (#0ea5e9) إلى بنفسجي (#764ba2)
- **اللكنة:** سماوي، أخضر، وردي
- **الخلفية:** تدرجات متحركة
- **الزجاج:** rgba(255, 255, 255, 0.1) مع ضبابية في الخلفية

### الرسوم المتحركة
- ✨ تلاشي عند تحميل الصفحة
- ✨ انزلاق للأعلى للبطاقات
- ✨ نبض للعناصر النشطة
- ✨ وميض لحالات التحميل
- ✨ انتقالات سلسة في جميع التفاعلات

### الطباعة
- **الخط:** Inter (Google Fonts)
- **الأوزان:** 300, 400, 500, 600, 700, 800

---

## 🔒 أفضل ممارسات الأمان

1. **لا تشارك مفتاحك السري أبدًا** - إنه فريد لجهازك
2. **استخدم كلمات مرور قوية** - اجمع بين الحروف والأرقام والرموز
3. **فعّل القفل البيومتري** - على تطبيق المصادقة الخاص بك
4. **احتفظ برموز الاسترداد** - في حال فقدان جهازك
5. **راجع الأجهزة بانتظام** - أزل الأجهزة غير المستخدمة
6. **استخدم HTTPS في الإنتاج** - لا ترسل الرموز عبر HTTP أبدًا

---

## 🚀 النشر الإنتاجي

### الواجهة الخلفية (Django)

1. **تحديث settings.py:**
   ```python
   DEBUG = False
   ALLOWED_HOSTS = ['yourdomain.com']
   SECRET_KEY = 'your-production-secret-key'
   ```

2. **استخدم PostgreSQL:**
   ```python
   DATABASES = {
       'default': {
           'ENGINE': 'django.db.backends.postgresql',
           'NAME': 'otp_db',
           'USER': 'your_user',
           'PASSWORD': 'your_password',
           'HOST': 'localhost',
           'PORT': '5432',
       }
   }
   ```

3. **النشر باستخدام Gunicorn:**
   ```bash
   pip install gunicorn
   gunicorn otp_core.wsgi:application --bind 0.0.0.0:8000
   ```

### الواجهة الأمامية (React)

1. **تحديث API URL في `src/services/api.js`:**
   ```javascript
   const API_BASE_URL = 'https://api.yourdomain.com/api/auth';
   ```

2. **البناء للإنتاج:**
   ```bash
   npm run build
   ```

3. **النشر على Netlify/Vercel:**
   - رفع مجلد `dist/`
   - تكوين عمليات إعادة التوجيه لـ SPA routing

---

## 📊 مخطط قاعدة البيانات

### نموذج TOTPDevice

| الحقل | النوع | الوصف |
|-------|------|-------------|
| id | Integer | المفتاح الأساسي |
| user | ForeignKey | رابط لنموذج المستخدم |
| name | CharField | اسم الجهاز |
| secret_key | CharField | سر TOTP (32 حرف) |
| is_active | Boolean | حالة الجهاز |
| last_verified_counter | BigInteger | منع هجمات إعادة التشغيل |
| created_at | DateTime | طابع وقت الإنشاء |
| last_used_at | DateTime | طابع وقت آخر استخدام |

---

## 🎯 التحسينات المستقبلية

- [ ] **تطبيق Android مخصص** - تطبيق مصادقة بعلامة تجارية
- [ ] **نسخ احتياطي SMS** - OTP احتياطي عبر الرسائل القصيرة
- [ ] **إشعارات البريد الإلكتروني** - تنبيه عند إضافة جهاز جديد
- [ ] **سجلات التدقيق** - تتبع جميع محاولات المصادقة
- [ ] **رموز الاسترداد** - رموز احتياطية لمرة واحدة
- [ ] **مصادقة بيومترية** - تكامل WebAuthn
- [ ] **لوحة تحكم المسؤول** - إدارة كافة المستخدمين والأجهزة
- [ ] **تحديد المعدل** - منع هجمات القوة الغاشمة

---

## 📝 الترخيص

هذا المشروع هو برنامج مملوك تم تطويره بواسطة **ZahranTeck**.

---

## 👨‍💻 المطور

**تم بناؤه بـ ❤️ بواسطة ZahranTeck**

للدعم أو الاستفسارات: demo@zahrantech.com

---

## 🙏 شكر وتقدير

- **Django** - إطار الويب للكماليين
- **React** - مكتبة JavaScript لبناء واجهات المستخدم
- **PyOTP** - مكتبة كلمة المرور لمرة واحدة لـ Python
- **Framer Motion** - مكتبة رسوم متحركة جاهزة للإنتاج
- **Tailwind CSS** - إطار عمل CSS قائم على الأدوات

---

<div align="center">

**⭐ إذا أعجبك هذا المشروع، يرجى منحه نجمة! ⭐**

صنع بـ 🔐 و ✨ بواسطة ZahranTeck

</div>
